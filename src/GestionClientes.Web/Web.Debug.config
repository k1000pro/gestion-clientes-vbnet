<?xml version="1.0" encoding="utf-8"?>
<!--
    Transformación aplicada al publicar en configuración Debug. No cambia nada: el Web.config
    base ya está preparado para desarrollo. Existe para dejar explícito que la variante de
    desarrollo es la base, y no un olvido.
-->
<configuration xmlns:xdt="http://schemas.microsoft.com/XML-Document-Transform">
</configuration>
